name = "forwardr-webhook"
main = "worker.js"
compatibility_date = "2024-01-01"

# Environment variables for the worker
# These will be set via Wrangler CLI or Cloudflare dashboard
[vars]
# FASTAPI_BACKEND_URL = "https://your-app.onrender.com"
# API_SECRET_KEY = "your-secret-key"

# Example secrets (set via: wrangler secret put SECRET_NAME)
# - API_SECRET_KEY: Secret key for authenticating with the backend
